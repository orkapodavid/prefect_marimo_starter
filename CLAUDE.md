# CLAUDE.md

## Temporary files

Write all temporary and scratch files to `tmp/` at the repo root. This directory is git-ignored. Never use `/tmp/` or other system-level directories for ephemeral outputs (downloaded pages, test workspaces, debug artifacts).
